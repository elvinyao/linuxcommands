# 机器学习 API 服务运行现状梳理

## 1. 核心技术栈与应用逻辑
* **应用类型**：Python 机器学习线上推理 API 服务。
* **主要依赖库**：`pandas`、`pyyaml`、`pyahocorasick`、`lightgbm`、`fastapi`。
* **并发配置**：Uvicorn 启动参数明确设置为 `workers=1`。
* **运行表现**：通过日志观察，系统当前是一条一条（串行）进行数据预处理和推理的。

## 2. 运行环境与基础配置对比

| 维度 | 环境 A (表现正常) | 环境 B (表现异常/极慢) |
| :--- | :--- | :--- |
| **云厂商** | AWS | OCI (Oracle Cloud) |
| **运行底座** | 原生 Docker 容器 | OKE (Kubernetes 集群) 中的 Pod |
| **镜像与基础配置**| 与环境 B 完全一致 | 与环境 A 完全一致 |
| **K8s 资源限制** | 不适用 | Deployment 的 resources 中 **未配置限制 (No CPU limits)** |

## 3. 核心症状与性能差异
* **整体速度**：OKE 端比 AWS 端 **慢 4 到 5 倍**。
* **变慢的环节**：并非单一环节卡顿，而是 **数据预处理** 和 **模型推理** 两个阶段全面变慢。

## 4. 关键资源监控数据差异（核心矛盾点）
这是目前最核心的客观现象差异，也是造成速度差异的直接物理表现：
* **AWS 端 (快)**：通过 `docker stats` 观察，虽然 Uvicorn 只有一个 worker，但容器的 **CPU 使用率最高可以飙升达到 200%**。
* **OKE 端 (慢)**：通过 Grafana 监控查看，即使在没有显式配置 CPU 限制的情况下，该 Pod 的 **CPU 峰值最多只能跑到 0.6 左右**，无法继续上升。

## 5. 现状总结
完全相同的代码、镜像和单 Worker 配置，在 AWS Docker 环境下能利用到相当于 2 个物理核心的算力（200%），运行流畅；而一模一样的程序搬到 OCI OKE 的 Kubernetes 环境中，CPU 实际最高利用率被莫名“锁死”在 0.6 左右，直接导致预处理和推理全链路变慢了 4-5 倍。
